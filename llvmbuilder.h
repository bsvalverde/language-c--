#pragma once

// #include "llvm/IR/IRBuilder.h"
// #include "llvm/IR/Module.h"

class LlvmBuilder {
public:
	LlvmBuilder();

	// buildSum(std::string left, std::string right);
private:
	// llvm::Module module;
	// llvm::IRBuilder<> builder;
	// llvm::LLVMContext globalContext;
};